    TEMPORARY; del after DEBUG SPRING SECURITY
+ added views for Spring Security enabling: admin\, shared\, user\index.html, _error, _index, _login
+ web.TmpSecurityController

TODO ENABLE SPRING SECURITY +login form; logout; error page
?Java config or xml?
? exception handling to ERROR page?

EXAMPLE 1:
https://github.com/dkrasot/demo-thyme-security/tree/master/src/main/webapp/WEB-INF

TODO write test for UploadController

TODO Handling exceptions
error.html + /error in LoginController

+ ???
    WebConfig - SimpleMappingExceptionResolver
    web.xml - error-page

EXAMPLE:
in SecurityConfig:
                    and().exceptionHandling().accessDeniedPage("/403")
in Controller:
                    @RequestMapping("/403")
                    public String accessDenied() {
                        return "errors/403";
                    }

TODO add RememberMe??

TODO tweets POST

    NOW LINKS
GET /
GET /tweets
GET POST /profile/signup
GET /profile/{username}
GET POST /login
GET /logout
GET /index x4

TODO NEXT LINKS

GET /tweets/{tweetId} TODO add tweet.html
GET /login?error or /login-error
GET POST /signup (change from /profile/signup ) TODO /profile/signUp to signUp
GET PUT /me (profile of auth user)
    for ADMIN:
GET /admin
GET POST /admin/userList
GET POST PUT /admin/userList/{username}
GET POST /admin/userList/{username}/tweetList
GET POST PUT /admin/userList/{username}/tweetList/{tweetId}

TODO enable EMAIL validation (Hibernate validate is n't working NOW)